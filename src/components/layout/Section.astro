---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<section class={`mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</section>
