---
// Indicador de scroll visual que se integra con scrollSnap
---

<div
  id="scroll-indicator"
  class="fixed top-1/2 right-6 z-40 hidden -translate-y-1/2 flex-col items-center opacity-100 transition-all duration-300 md:flex"
>
  <!-- Container con fondo -->
  <div
    class="relative rounded-lg p-4 backdrop-blur-md"
    style="background: color-mix(in srgb, var(--background) 80%, transparent); border: 1px solid color-mix(in srgb, var(--border) 60%, transparent);"
  >
    <!-- Dots de navegación -->
    <div class="flex flex-col gap-3">
      <button
        class="scroll-dot active group relative h-8 w-3 rounded-full transition-all duration-300 hover:scale-110 focus:outline-none"
        data-section="0"
        aria-label="Ir al inicio"></button>
      <button
        class="scroll-dot group relative h-3 w-3 rounded-full transition-all duration-300 hover:scale-110 focus:outline-none"
        data-section="1"
        aria-label="Ir al contenido"></button>
    </div>
  </div>
</div>

<style>
  .scroll-dot {
    background: color-mix(in srgb, var(--muted-foreground) 30%, transparent);
    border: 1px solid color-mix(in srgb, var(--border) 40%, transparent);
    position: relative;
    z-index: 2;
    cursor: pointer;
  }

  .scroll-dot:hover {
    background: color-mix(in srgb, var(--muted-foreground) 50%, transparent);
    border-color: color-mix(in srgb, var(--primary) 60%, transparent);
  }

  .scroll-dot.active {
    background: var(--primary);
    border-color: var(--primary);
    height: 2rem; /* h-8 */
    width: 0.75rem; /* w-3 */
    cursor: default;
    pointer-events: none;
  }

  .scroll-dot:not(.active) {
    height: 0.75rem; /* h-3 */
    width: 0.75rem; /* w-3 */
  }

  /* Efecto de hover en el contenedor */
  #scroll-indicator:hover .scroll-dot:not(:hover):not(.active) {
    opacity: 0.5;
  }
</style>
